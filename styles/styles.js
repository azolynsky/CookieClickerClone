import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  countDisplay: {
    textAlign: 'center', fontSize: 36, color: 'white', backgroundColor:'green', paddingTop:50, paddingBottom:20
  }
})

export default styles